# CMP rollback 13 Android GUI 验证

## 环境与产物

- 设备：`emulator-5554`，AVD `Pixel_10_Pro_XL`，`sdk_gphone16k_arm64`，2560×1600 横屏。
- 包：`me.rerere.rikkahub.debug`。
- 测试技能：`cmp-rollback-13-android`；固定 frontmatter、`SKILL.md` 正文 v1，以及 `docs/guide.md` 辅助文件。全过程没有网络或模型请求。
- 完整回归先在预最终 Android 包（SHA-256 `e86ce0e8b43d0d517ad49a4fe8f0d6e13762fc4b322d3f0e9efcbbe05bcd7c9c`）完成；父 agent 随后提供冻结最终包，故另做最终包补验。
- 最终 APK：`app/build/outputs/apk/debug/app-arm64-v8a-debug.apk`，本地实算 SHA-256 与 `build-artifacts.json` 一致：`79e2f78015a5ad155107208376e09f03e6e38ed79506ce3d48e4ccae158f8796`。

## 完整回归（预最终包）

| 操作 | 预期 | 实际 |
| --- | --- | --- |
| 经系统文件选择器导入测试 ZIP | 技能出现在 Agent Skills 列表 | 导入成功；最终包补验的同一路径与成功提示见 `09-final-apk-imported.png`。 |
| 打开主文档 `SKILL.md` | 读到固定 frontmatter 和正文 v1 | 实际显示 `Main document v1.`，见 `13-main-document-v1.png`。 |
| 打开 `docs/guide.md` | 读到辅助文件 v1 | 实际显示 `Auxiliary document v1.`，见 `16-auxiliary-document-v1.png`。 |
| 编辑辅助文件为 v2，强制停止并冷启动，再从实际编辑入口读取 | 读回 v2，且应用恢复到可操作的新对话页 | 实际读到 `Auxiliary document v2.`，见 `05-cold-start-v2-readback.png`；冷启动完成页见 `32-cold-start-current-ready.png`。 |
| 在第一项测试助手的 Extensions > Skills 勾选该技能，再冷启动复核 | 勾选保持 | 技能开关仍为已启用，见 `06-cold-start-selection-persisted.png`。 |
| 删除测试技能 | 全局技能列表和该助手的选择均清理 | 全局列表显示 `No Skills`，见 `07-old-build-deletion-list-cleared.png`；助手 Skills 页显示 `No skills available`，见 `08-old-build-deletion-assistant-selection-cleared.png`。 |

完整回归结论：通过。每次 GUI 操作前均先观察当前界面；所有报告引用的原始截图均已用 `view_image` 逐张确认。首次文件选择器唤起曾回到应用列表，未作为通过依据；按同一路径重试后成功导入。

## 最终包补验

| 操作 | 预期 | 实际 |
| --- | --- | --- |
| 安装并启动哈希已核对的最终 APK；经系统文件选择器导入 ZIP | 导入成功 | 显示 `Skill imported: cmp-rollback-13-android` 且列表出现技能，见 `09-final-apk-imported.png`。 |
| 打开技能目录树 | 同时可见根 `SKILL.md` 和 `docs/guide.md` | `docs` 已展开，两个文件均可见，见 `11-final-apk-directory-expanded.png`。 |
| 通过实际文件读取入口打开 `docs/guide.md` | 读到导入 ZIP 的辅助文件 v1 | 实际显示 `Auxiliary document v1.`，见 `12-final-apk-guide-read.png`。 |
| 删除最终包补验导入的临时技能 | 列表无残留 | 当时列表显示 `No Skills`，见 `13-final-apk-cleanup-list-cleared.png`；该截图不作为最终清理依据，后续复核发现本轮夹具仍在实际技能目录中。 |

最终包补验结论：导入、目录查看和辅助文件读取通过。该补验覆盖详情页经 `Manager.resolveSkillFile` 读取辅助文件的实际 GUI 链路。

## 状态与清理

最终清理复核发现：最终包补验的早期列表截图为空，但实际 `files/skills` 仍有本轮夹具。已通过 **Agent Skills → Delete → 确认** 重现删除，不使用文件系统直接删除掩盖该现象。

| 复核操作 | 预期 | 实际 |
| --- | --- | --- |
| GUI 确认删除后读取全局列表 | 无技能 | `No Skills`，见 `14-cleanup-agent-skills-empty.png`。 |
| 在已打开的助手详情页等待稳定 | 既有 VM 可能维持 init 时的技能列表 | 仍显示已删除技能但开关关闭；此为已核对的 init-only 缓存行为。 |
| 冷启动并新建助手详情 VM 后读取 Skills | 无技能 | `No skills available`，见 `15-cleanup-cold-start-assistant-skills-empty.png`。 |

清理证据仅记录布尔、计数和哈希：

- `actual_skill_file_count=0`
- `skills_manifest_equal=true`
- `skills_manifest_sha256=e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855`
- `all_assistants_enabledSkills_fixture_reference_count=0`
- `download_fixture_removed=true`
- `debug_app_stopped=true`
- `avd_stopped=true`
- `preference_equal=false`
- `changed_key_count=1`
- `changed_keys=[launch_count]`（正常启动计数）
- `preference_restore_required=false`
- `private_cleanup_completed=true`

偏好仅在本机私有副本中解析比较，未显示其值或凭据，未恢复任何字段；没有清空应用数据或覆盖其他变更，且未操作桌面 PID `99566`。仓库目录仅保留本报告引用且已逐张 `view_image` 确认的原始截图；其余本轮私有夹具、备份、中间截图和布局文件已在 AVD 停止后删除。
