CMP rollback 14 Android avatar diagnostic summary

Frozen-package failed attempts: both GUI crop confirmations returned to Basic Settings
with the default avatar and created 0-byte upload JPG files. The retained filtered
log had only the system Photo Picker selection line; it had no AndroidRuntime,
CropLauncher, or RikkaHub error. The final package then produced the non-empty
653-byte avatar listed in README.md, persisted it through cold start, and left no
crop_output cache file.

No credentials, provider configuration, or user setting values are present here.
