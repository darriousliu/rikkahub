无效工具消息的节点清理计算：原样迁移验证（2026-09-09）
基线：51c01a209；策略 PRESERVE，适用性 SUPPORTED。

基线命令：
./gradlew :composeApp:jvmTest :composeApp:testAndroidHostTest :composeApp:iosSimulatorArm64Test --console=plain
BUILD SUCCESSFUL in 1s，退出码 0。Android host 144、JVM 151、iOS Simulator 144 项。

迁移后命令：
./gradlew :composeApp:jvmTest :composeApp:testAndroidHostTest :composeApp:iosSimulatorArm64Test :composeApp:compileCommonMainKotlinMetadata :composeApp:compileKotlinIosArm64 :app:compileDebugKotlin :app:testDebugUnitTest --tests me.rerere.rikkahub.service.ChatServiceTest --console=plain
BUILD SUCCESSFUL in 40s，退出码 0。
testAndroidHostTest: tests=151, failures=0, errors=0, skipped=0
jvmTest: tests=159, failures=0, errors=0, skipped=0
iosSimulatorArm64Test: tests=151, failures=0, errors=0, skipped=0
Android ChatServiceTest: tests=1, failures=0, errors=0, skipped=0
总计 462 次；新增 7 项 common 测试各在 Android/JVM/iOS 执行，另有 1 项 JVM SQLite 重开用例，新增 22 次。
测试结果无失败、错误或跳过。

实际生产编译任务：
> Task :composeApp:compileCommonMainKotlinMetadata
> Task :composeApp:compileKotlinIosArm64
> Task :composeApp:compileKotlinIosSimulatorArm64
> Task :composeApp:compileAndroidMain
> Task :composeApp:compileKotlinJvm
> Task :app:compileDebugKotlin
iOS arm64 本项只验证编译。

契约覆盖：
- 五种工具审批状态乘以空/非空输出；Auto/Pending 无输出则移除，可恢复审批或已有输出则保留。
- 混合未解决与可恢复工具时按原 any 规则保留整条；已执行的可恢复工具不阻止其他未解决工具触发移除。
- 首/中/末分支移除，保留数字索引减一和越界归零；每轮只检查当前分支，不递归处理新选中的分支。
- 按当前消息 ID 移除同一节点内所有重复 ID；其他节点和未选中分支原样保留。
- 空结果节点移除；会话/节点 ID、收藏、顺序、译文、工具元数据及保留消息文件引用保持。
- 输入已为空的节点和非法索引保留原 IllegalStateException 及消息，不增加预修复。
- 空会话、无工具消息、未选中未执行工具及工具输出中的嵌套未执行工具均保持原行为。

SQLite 集成：使用既有 Repository 插入原会话，再保存共享计算的结果，关闭后重新打开同一数据库。
完整会话相等；清理后的节点、索引、审批状态、译文、元数据和文件引用保持，移除消息退出 FTS，
保留的当前/备选分支仍可搜索。仅使用虚构文件 URI，没有物理附件删除。
SQLite 连接在 finally 中关闭并删除本用例独占的临时目录。

源码对比：共享函数的计算块与原 checkInvalidMessages 逐字相同（仅去除外层缩进），
末尾原 conversation.copy 改为返回值。Android 原方法只用共享函数替换计算，仍读取一次会话并调用一次
原 updateConversation，删除一个无用 import；ChatService 其余内容逐字未改。
ChatRuntime、SharedChatRuntime、UI、Repository、DAO、FTS、平台文件处理、依赖及配置未修改。
原 allToolsExecuted 分支、重复 ID 处理、单次遍历和错误行为均保留，无新增校验/锁/状态机。
新增公开 API：buildConversationAfterInvalidMessageCleanup(Conversation): Conversation，供 app 跨模块调用。
SharedChatRuntime 未增加清理触发，本项共享的是计算部分。

Android 编译调用：javap -c -p 确认原 private 包装方法的顺序为
getConversationFlow -> StateFlow.getValue -> InvalidMessageCleanupKt.buildConversationAfterInvalidMessageCleanup
-> 原 updateConversation；指令片段见 android-call.txt。

GUI 判断：无需 GUI。原界面、状态订阅、生命周期和平台更新/文件处理/保存接线不变，
三端边界、SQLite 持久化和实际编译调用证据覆盖本次纯同步计算迁移。
未执行完整应用 GUI，不将 SQLite 测试记为实际附件删除。没有模型请求、临时生产日志或设备安装。
SQLite 专用临时目录自动清理；临时构建日志保留本记录后删除。
