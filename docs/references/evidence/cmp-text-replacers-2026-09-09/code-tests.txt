Workspace 文本替换器：原样迁移验证（2026-09-09）
基线：3e3ad7545；策略 PRESERVE，适用性 SUPPORTED。

基线命令：
./gradlew :composeApp:jvmTest :composeApp:testAndroidHostTest :composeApp:iosSimulatorArm64Test :app:testDebugUnitTest --tests me.rerere.rikkahub.data.ai.tools.TextReplacersTest --tests me.rerere.rikkahub.service.ChatServiceTest --console=plain
BUILD SUCCESSFUL in 5s，退出码 0。
共享 Android host 151、JVM 159、iOS Simulator 151；app TextReplacersTest 14、ChatServiceTest 1，全部通过。

迁移后命令：
./gradlew :composeApp:jvmTest :composeApp:testAndroidHostTest :composeApp:iosSimulatorArm64Test :composeApp:compileCommonMainKotlinMetadata :composeApp:compileKotlinIosArm64 :app:compileDebugKotlin :app:testDebugUnitTest --tests me.rerere.rikkahub.service.ChatServiceTest --console=plain
BUILD SUCCESSFUL in 39s，退出码 0；432 个任务，62 executed，370 up-to-date。
testAndroidHostTest: tests=165, failures=0, errors=0, skipped=0
jvmTest: tests=173, failures=0, errors=0, skipped=0
iosSimulatorArm64Test: tests=165, failures=0, errors=0, skipped=0
Android ChatServiceTest: tests=1, failures=0, errors=0, skipped=0
总计 504 次测试执行。原 TextReplacersTest 的 14 项用例移到 commonTest 后各在三端通过，共执行 42 次。
没有新增测试逻辑，app 中不再重复保留原测试。测试 XML 无失败、错误或跳过。

实际生产编译任务：
> Task :composeApp:compileCommonMainKotlinMetadata
> Task :composeApp:compileKotlinIosSimulatorArm64
> Task :composeApp:compileKotlinIosArm64
> Task :composeApp:compileAndroidMain
> Task :composeApp:compileKotlinJvm
> Task :app:compileDebugKotlin
iOS arm64 本项只验证编译。

沿用的测试覆盖：精确单处替换、非重叠计数、replaceAll、多处歧义、未找到/空旧文本异常；
逐行宽松匹配、缩进和相对缩进恢复、CRLF/LF 及尾部换行、逐行多处歧义、纯空白不宽松匹配；
首尾锚点匹配和至少三行要求。输入与原预期结果保持。

生产代码只有 TextReplacers.kt 从 app 移至 commonMain，194 行逐字节一致。
迁移前后 SHA-256 相同：2cba69101acff495ca4d2e1592810869a32bf0b3aebffcbf37e5d8655a06d2a9
测试仅替换三个 JUnit import，以及 assertThrows(IllegalArgumentException::class.java)
为 assertFailsWith<IllegalArgumentException>；其余内容逐字不变。
包名、类、接口、函数、私有辅助定义、默认参数、原校验及异常不变；不新增生产逻辑或依赖。

WorkspaceTools.kt 与基线逐字相同，参数、审批、读写、异常包装、JSON 和 diff metadata 保持。
javap 确认原 Android 编辑工具按原顺序读取文件、调用 TextReplacersKt.replaceText$default、
读取替换结果并写回，仍使用 ExactReplacer.name；片段与类位置见 android-call.txt。
app 编译目录中原 TextReplacersKt / ExactReplacer 已移除，共享 Android/JVM 类产物存在。
本项共享文本算法，Workspace Rootfs、文件访问和 shell 沿用现有平台边界。

GUI 判断：无需 GUI。生产文件原样移动，原工具与平台接线、文件读写和界面展示未变；
原有行为测试在三端通过，Android 编译调用证据覆盖迁移后的链接。
未执行完整 Workspace Rootfs 文件编辑或完整应用 GUI，没有临时生产日志、设备安装或模型请求。
测试只在内存中替换字符串；构建临时日志保留证据后删除。
