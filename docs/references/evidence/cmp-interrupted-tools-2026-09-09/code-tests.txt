被打断工具调用的收尾处理：原样迁移验证（2026-09-09）
基线：387778613；策略 PRESERVE，适用性 SUPPORTED。

基线命令：
./gradlew :composeApp:jvmTest :composeApp:testAndroidHostTest :composeApp:iosSimulatorArm64Test --console=plain
BUILD SUCCESSFUL in 1s，退出码0。Android host 128、JVM 134、iOS Simulator 128 项。

迁移后命令：
./gradlew :composeApp:jvmTest :composeApp:testAndroidHostTest :composeApp:iosSimulatorArm64Test :composeApp:compileCommonMainKotlinMetadata :composeApp:compileKotlinIosArm64 :app:compileDebugKotlin :app:testDebugUnitTest --tests me.rerere.rikkahub.service.ChatServiceTest --console=plain
BUILD SUCCESSFUL in 52s，退出码0。
testAndroidHostTest: tests=135, failures=0, errors=0, skipped=0
jvmTest: tests=142, failures=0, errors=0, skipped=0
iosSimulatorArm64Test: tests=135, failures=0, errors=0, skipped=0
Android ChatServiceTest: tests=1, failures=0, errors=0, skipped=0
总计413次；新增7项common测试各在Android/JVM/iOS执行，另有1项JVM SQLite重开用例，共新增22次。

本次实际执行的生产编译任务：
:composeApp:compileKotlinJvm
:composeApp:compileAndroidMain
:composeApp:compileKotlinIosArm64
:composeApp:compileKotlinIosSimulatorArm64
:composeApp:compileCommonMainKotlinMetadata
:app:compileDebugKotlin

覆盖：五种审批状态的无输出工具均保留原取消JSON/Denied原因；已执行工具及嵌套输出保持；
消息/推理结束时间按原规则补齐；保留前面节点、未选中分支、附件、消息及会话元数据；
空会话/无需收尾不保存；重复收尾只保存一次；重读最新会话；重复消息ID的原替换行为；
空末节点/非法索引的原异常；保存异常和取消不重试且原样传播。
SQLite用例通过现有Repository保存，关闭连接后重新打开同一文件，验证完整会话相等、
取消结果与结束时间、元数据、分支和文件引用保持，当前/未选中分支FTS均可检索。没有物理附件操作。

源码对比：两段方法体逐字保留（去除外层缩进），仅将private收尾成员改为公开ChatRuntime扩展。
Android仅删除两个原定义和finishPendingTools import；原sendMessage/stopGeneration的cancel/join/收尾顺序完全不变。
ChatRuntime接口、SharedChatRuntime、UIMessage、UI、存储、平台集成和依赖未改；新增common文件无Java/Android API。

编译调用检查使用javap -c -p，两个产物均包含invokestatic InterruptedToolCompletionKt.finishInterruptedPendingTools：
app/build/intermediates/built_in_kotlinc/debug/compileDebugKotlin/classes/me/rerere/rikkahub/service/ChatService.class（stopGeneration）
app/build/intermediates/built_in_kotlinc/debug/compileDebugKotlin/classes/me/rerere/rikkahub/service/ChatService$sendMessage$job$1.class

GUI判断：无需GUI。保留原触发、状态接线、生命周期与保存链路，以运行时契约、SQLite和实际编译调用验证本次搬迁。
未执行完整应用GUI，未增加其他平台停止收尾入口，无模型调用或临时生产日志。
SQLite测试连接关闭后自动删除专用目录；Gradle临时日志清理后提交。
