用户输入正则预处理：原样迁移验证（2026-09-09）
基线：44b376a1c；策略 PRESERVE，适用性 SUPPORTED。

基线命令：
./gradlew :composeApp:jvmTest :composeApp:testAndroidHostTest :composeApp:iosSimulatorArm64Test --console=plain
BUILD SUCCESSFUL in 1s，退出码0。Android host 122、JVM 128、iOS Simulator 122 项。

迁移后命令：
./gradlew :composeApp:jvmTest :composeApp:testAndroidHostTest :composeApp:iosSimulatorArm64Test :composeApp:compileCommonMainKotlinMetadata :composeApp:compileKotlinIosArm64 :app:compileDebugKotlin :app:testDebugUnitTest --tests me.rerere.rikkahub.service.ChatServiceTest --console=plain
BUILD SUCCESSFUL in 43s，退出码0。
testAndroidHostTest: tests=128, failures=0, errors=0, skipped=0
jvmTest: tests=134, failures=0, errors=0, skipped=0
iosSimulatorArm64Test: tests=128, failures=0, errors=0, skipped=0
Android ChatServiceTest: tests=1, failures=0, errors=0, skipped=0
总计391次；新增6项common测试各在Android/JVM/iOS执行，共新增18次。

本次实际执行的生产编译任务：
:composeApp:compileKotlinJvm
:composeApp:compileAndroidMain
:composeApp:compileKotlinIosArm64
:composeApp:compileKotlinIosSimulatorArm64
:composeApp:compileCommonMainKotlinMetadata
:app:compileDebugKotlin

覆盖行为：启用/作用域/视觉规则过滤，替换顺序、捕获组、多文本、中文和换行，元数据及非文本对象引用，
不递归替换工具结果文本，非法正则和错误替换分组后的继续处理，空输入/无规则/无匹配，保留空白替换结果。
异常测试会触发现有replaceRegexes的printStackTrace；未添加生产日志，也未修改原异常处理。

源码对比：原方法仅移除外层缩进与private；Android仅删除原定义及三个未使用的import，两个调用点原样保留。
SharedChatRuntime仅以共享调用替换等价内联map并删除两个未使用的import，其他方法体逐字不变。
新common文件无Java/Android API；原String.replaceRegexes、AssistantRegex、缓存和异常处理未变。

编译调用检查使用javap -c -p，以下产物均包含invokestatic UserInputPreprocessorKt.preprocessUserInputParts：
app/build/intermediates/built_in_kotlinc/debug/compileDebugKotlin/classes/me/rerere/rikkahub/service/ChatService.class（editMessage）
app/build/intermediates/built_in_kotlinc/debug/compileDebugKotlin/classes/me/rerere/rikkahub/service/ChatService$sendMessage$job$1.class
composeApp/build/classes/kotlin/jvm/main/me/rerere/rikkahub/service/SharedChatRuntime$sendMessage$1.class
composeApp/build/classes/kotlin/android/main/me/rerere/rikkahub/service/SharedChatRuntime$sendMessage$1.class

GUI判断：无需GUI。纯计算及等价调用替换，发送/编辑入口、状态接线、平台集成、生命周期、存储和打包均不变。
未执行GUI，无模型请求、临时生产日志或临时测试数据。Gradle临时日志清理后提交。
