聊天消息翻译迁移自动化验证（2026-09-09）
迁移基线：4dab04e3；验证对象：本次翻译迁移工作树。

命令：
./gradlew :composeApp:jvmTest :composeApp:testAndroidHostTest :composeApp:iosSimulatorArm64Test --console=plain
结果：BUILD SUCCESSFUL in 19s；退出码 0。

从 composeApp/build/test-results/<target>/TEST-*.xml 汇总：
Android host：121 tests，0 failures，0 errors，0 skipped。
JVM：137 tests，0 failures，0 errors，0 skipped。
iOS Simulator arm64：121 tests，0 failures，0 errors，0 skipped。

本项新增用例：
TextTranslationGeneratorTest：每目标 9 项通过。
MessageTranslationManagerTest：每目标 15 项通过。
MessageTranslationPersistenceTest：JVM 5 项通过。
MessageTranslationConcurrencyTest：JVM 1 项通过。
新增用例执行合计：78 次通过。

Provider 为无网络 fake；SQLite 测试使用真实 Room/Bundled SQLite。
并发用例用两条真实线程和 latch，暂停旧 partial 更新，再并发清除，确认最终内存和磁盘为空。
本项代码测试无真实模型 token 消耗。

编译/Android 回归命令：
./gradlew :composeApp:compileCommonMainKotlinMetadata :composeApp:compileAndroidMain :composeApp:compileKotlinJvm :composeApp:compileKotlinIosArm64 :composeApp:compileKotlinIosSimulatorArm64 :app:compileDebugKotlin :app:testDebugUnitTest --tests me.rerere.rikkahub.service.ChatServiceTest --console=plain
结果：BUILD SUCCESSFUL in 14s；退出码 0。
Android ChatServiceTest：1 test，0 failures，0 errors，0 skipped。

清理临时 GUI seed 源集后恢复正常 JVM 配置：
./gradlew :composeApp:jvmTest --console=plain
BUILD SUCCESSFUL in 9s；137 tests，0 failures，0 errors，0 skipped。
测试报告及已编译测试类中无 GuiTranslationSeedTest 残留。
